export type formType = {
    email: string;
    password: string;
};

export type variableType = {
    email: string;
    password: string;
};

export type responseType = {
    message: string;
    token: string;
};
